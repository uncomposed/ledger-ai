{
  "$id": "changeset.applied.v1",
  "type": "object",
  "additionalProperties": false,
  "required": ["correlation_id", "changeset_id", "task_id", "entity_id", "approved_by_actor_id", "produced"],
  "properties": {
    "correlation_id": { "type": "string" },
    "changeset_id": { "type": "string", "format": "uuid" },
    "task_id": { "type": "string", "format": "uuid" },
    "entity_id": { "type": "string", "format": "uuid" },
    "approved_by_actor_id": { "type": "string", "format": "uuid" },
    "produced": {
      "type": "object",
      "additionalProperties": false,
      "required": ["changeset_ids"],
      "properties": {
        "changeset_ids": { "type": "array", "items": { "type": "string", "format": "uuid" } }
      }
    }
  }
}

