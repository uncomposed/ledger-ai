{
  "$id": "changeset.proposed.v1",
  "type": "object",
  "additionalProperties": false,
  "required": ["correlation_id", "changeset_id", "task_id", "entity_id", "proposed_by_actor_id", "risk_level", "produced"],
  "properties": {
    "correlation_id": { "type": "string" },
    "changeset_id": { "type": "string", "format": "uuid" },
    "task_id": { "type": "string", "format": "uuid" },
    "entity_id": { "type": "string", "format": "uuid" },
    "proposed_by_actor_id": { "type": "string", "format": "uuid" },
    "risk_level": { "type": "string" },
    "produced": {
      "type": "object",
      "additionalProperties": false,
      "required": ["changeset_ids"],
      "properties": {
        "changeset_ids": { "type": "array", "items": { "type": "string", "format": "uuid" } }
      }
    }
  }
}

